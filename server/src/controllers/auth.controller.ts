import { asyncHandler } from "../middlewares/asyncHandler.middleware";

export const loginUserController = asyncHandler(async () => {});

export const registerUserController = asyncHandler(async () => {});
